import javafx.scene.image.Image;
import javafx.scene.paint.Color;

public class ColorBombBrick extends SpecialBricks
{
    public ColorBombBrick(Color color, int row, int col, Image image)
    {
        super(color, row, col, image);
    }
}
