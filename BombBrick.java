import javafx.scene.paint.Color;
import javafx.scene.image.Image;

public class BombBrick extends SpecialBricks
{
    public BombBrick(Color color, int row, int col, Image image)
    {
        super(color, row, col, image);
    }
}
