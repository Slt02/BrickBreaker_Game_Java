import javafx.scene.image.Image;
import javafx.scene.paint.Color;

public class SuffleBrick extends SpecialBricks
{  
    public SuffleBrick(Color color, int row, int col, Image image)
    {
        super(color, row, col, image);
    }
}
