import javafx.scene.image.Image;
import javafx.scene.paint.Color;

public class NewLineBrick extends SpecialBricks
{
    public NewLineBrick(Color color, int row, int col, Image image)
    {
        super(color, row, col, image);
    }
}
